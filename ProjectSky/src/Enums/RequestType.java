package Enums;

public enum RequestType {
	/*
	 * ====================================================================================
	 * Query String을 어떤 방법으로 전달할 지를 구분한다.
	 * POST 방식 : POST
	 * GET 방식 : GET
	 * ====================================================================================
	 */
	POST, GET
}
